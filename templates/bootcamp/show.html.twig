{% extends 'base.html.twig' %}

{% block title %}Bootcamp{% endblock %}

{% block body %}
    <h1>Bootcamp</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ bootcamp.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ bootcamp.name }}</td>
            </tr>
            <tr>
                <th>Term</th>
                <td>{{ bootcamp.term }}</td>
            </tr>
            <tr>
                <th>Place</th>
                <td>{{ bootcamp.place }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_bootcamp_index') }}">back to list</a>

    <a href="{{ path('app_bootcamp_edit', {'id': bootcamp.id}) }}">edit</a>

    {{ include('bootcamp/_delete_form.html.twig') }}
{% endblock %}
